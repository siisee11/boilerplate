{
  "name": "next-nest-boilerplate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "yarn workspaces foreach --all -ptvi run dev",
    "fe": "yarn workspace frontend",
    "be": "yarn workspace backend"
  },
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "yarn@4.4.0"
}
